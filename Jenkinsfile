pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project - automatic trigger test!'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }

        stage('Finish') {
            steps {
                echo 'Pipeline completed successfully!'
            }
        }
    }
}
